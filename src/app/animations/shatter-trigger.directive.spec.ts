import { ShatterTriggerDirective } from './shatter-trigger.directive';

describe('ShatterTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ShatterTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
