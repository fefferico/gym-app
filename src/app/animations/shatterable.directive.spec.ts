import { ShatterableDirective } from './shatterable.directive';

describe('ShatterableDirective', () => {
  it('should create an instance', () => {
    const directive = new ShatterableDirective();
    expect(directive).toBeTruthy();
  });
});
