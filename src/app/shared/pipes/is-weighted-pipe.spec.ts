import { IsWeightedPipe } from './is-weighted-pipe';

describe('IsWeightedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsWeightedPipe();
    expect(pipe).toBeTruthy();
  });
});
